{
  "name": "my-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./dist/index.js",
    "dev": "tsx watch ./src/index.ts --conditions=typescript",
    "build": "tsc && npx tsc-alias -p tsconfig.json"
  },
  "dependencies": {
    "printer": "workspace:*"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "app/**"
  ],
  "devDependencies": {
    "@types/node": "^24.0.4",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.3"
  }
}
